- hosts: "elasticsearch-01"
  become: true
  roles:
    - { role: setup_elasticsearch, es_instance_name: "elasticsearch-01", es_config: { discovery.zen.ping.unicast.hosts: "10.0.5.8", network.host: "0.0.0.0", cluster.name: "srkay_es", http.port: 9200, transport.tcp.port: 9300, node.data: true, node.master: true } }
  vars:
    es_heap_size: "2g"
    es_cluster_name: "srkay_es"

- hosts: "elasticsearch-02"
  become: true
  roles:
    - { role: setup_elasticsearch, es_instance_name: "elasticsearch-02", es_config: { discovery.zen.ping.unicast.hosts: "10.0.5.8", network.host: "0.0.0.0", cluster.name: "srkay_es", http.port: 9200, transport.tcp.port: 9300, node.data: true, node.master: false } }
  vars:
    es_heap_size: "2g"
    es_cluster_name: "srkay_es"

